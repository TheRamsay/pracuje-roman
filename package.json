{
  "name": "pracuje-roman",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm --prefix packages/core run build && npm --prefix packages/db run build && npm --prefix apps/worker run build && npm --prefix apps/web run build",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:worker": "npm --prefix apps/worker run dev",
    "test": "npm --prefix packages/core run test && npm --prefix apps/web run test",
    "db:generate": "npm --prefix packages/db run db:generate",
    "db:migrate": "npm --prefix packages/db run db:migrate"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "drizzle-kit": "^0.31.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
